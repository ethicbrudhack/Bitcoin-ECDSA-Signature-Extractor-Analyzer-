# Bitcoin-ECDSA-Signature-Extractor-Analyzer-
 This utility extracts **ECDSA signature components (r, s)** from raw transaction hexadecimal data   using **regex-based DER decoding**. It allows researchers to isolate signature values for deeper   ECDSA vulnerability analysis (e.g. nonce reuse, identical r detection, or malformed DER checks).
